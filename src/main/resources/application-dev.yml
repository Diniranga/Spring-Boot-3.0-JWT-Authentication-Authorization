spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devdb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    root: INFO
